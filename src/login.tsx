import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { Button } from '@material-ui/core';

ReactDOM.render(<Button href="/auth">Twitterアカウントでログインする</Button>, document.getElementById('root'));
